const nota1 = parseInt(require('Digite a primeira nota'));
const nota2 = parseInt(require('Digite a segunda nota'));
const nota3 = parseInt(require('Digite a terceira nota'));

const media = (nota1 + nota2 + nota3) / 3;
console.log(`A média é ${media}`);

